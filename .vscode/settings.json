// Platzieren Sie Ihre Einstellungen in dieser Datei, um Standard- und Benutzereinstellungen zu überschreiben.
{
    "emeraldwalk.runonsave": {
        "autoClearConsole": true,
        "commands": [
            {
                "match": "scad-parser.ne",
                "cmd": "nearleyc ${file} > ${fileDirname}/grammar.js 2> ${fileDirname}/${fileBasenameNoExt}.err && nearley-railroad ${file} -o ${fileDirname}/${fileBasenameNoExt}-grammar.html"
            }
        ]
    }
}