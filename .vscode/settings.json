// Platzieren Sie Ihre Einstellungen in dieser Datei, um Standard- und Benutzereinstellungen zu überschreiben.
{
    "emeraldwalk.runonsave": {
        "autoClearConsole": true,
        "commands": [
            {
                "match": "grammar.ne",
                "cmd": "nearleyc ${file} > ${fileDirname}/${fileBasenameNoExt}.js 2> ${fileDirname}/${fileBasenameNoExt}.err && rm ${fileDirname}/${fileBasenameNoExt}.err"
            }
        ]
    }
}